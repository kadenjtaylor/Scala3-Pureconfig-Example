## Example Usage of Pureconfig for Scala3

## Usage

Run `Main.scala` which contains a limited example

```bash
sbt run
```

Run `MySuite.scala`, which verifies preference of test configs

```bash
sbt test
```
